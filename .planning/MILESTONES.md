# Milestones

## v1.0 MVP (Shipped: 2026-02-24)

**Phases completed:** 9 phases, 28 plans, 0 tasks

**Key accomplishments:**
- Bootstrapped a mobile-first PixiJS + Vite runtime shell with portrait-safe viewport behavior.
- Added FestivalMap contracts plus normalization loader logic with passing coordinate tests.
- Delivered config-driven stage/spawn rendering layers with resize-safe map reprojection.
- Implemented typed artist lifecycle entities with deterministic drift, timer expiry, and bounds-miss transitions.
- Built spawn/timer systems that enforce active caps, cadence windows, and timeout-driven miss events.
- Integrated phase runtime loop with artist/timer placeholder rendering and 3-life fail-state tracking.
- Implemented artist-targeted touch session capture with deterministic single-path lifecycle events.
- Built spline smoothing and deterministic path-finalization with stage snap targeting output.
- Introduced typed path lifecycle state and renderer support for active and invalid-fading route visuals.
- Integrated end-to-end draw-to-follow loop with path reassignment and ETA warning overlays in runtime.
- Implemented deterministic stage occupancy and queue lifecycle that resolves arrivals into performance completions.
- Implemented exact tier-stage scoring matrix and deterministic cumulative score event tracking.
- Integrated score/lives/level HUD and event-driven delivery-miss feedback into the runtime loop.
- Implemented collision chat-delay sessions with deterministic resume and blocked-state path queueing support.
- Implemented level-gated distraction delays with resolved map zones and dedicated distraction rendering layer.
- Integrated collision/distraction runtime flow with hazard overlays and blocked-state reroute application on resume.
- Implemented per-stage combo chaining with capped multiplier scoring integrated into runtime score events.
- Added stage-linked combo badges and combo-aware HUD/score popups with end-to-end runtime wiring.
- Introduced manager-owned session lifecycle with deterministic level fail/retry/advance transitions.
- Implemented seeded per-attempt level progression and bounded randomization for repeatable variety.
- Added actionable non-gameplay overlays for menu, level fail/complete, and festival completion.
- Implemented local persistence for progress, best scores, and settings, integrated into session flow and summaries.
- Integrated authored Gov Ball map content and asset-aware map/distraction rendering with strict schema validation.
- Replaced placeholder artist rendering with sprite-state visuals and wired gameplay audio cues through a safe runtime audio manager.
- Implemented explicit boot/festival bundle manifests and lifecycle controls with main-flow integration and regression tests.
- Added runtime telemetry, adaptive quality scaling hooks, and a reproducible profiling command with baseline report output.
- Expanded automated coverage for runtime fail/complete boundaries, hazard/input edge cases, and session/content integrity loops.
- Added repeatable soak execution, formalized release gates, and completed the final Phase 9 verification/sign-off report.

---


---
phase: 05-collisions-and-distractions
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/rendering/HazardOverlayRenderer.ts
  - src/rendering/HazardOverlayRenderer.test.ts
  - src/systems/PathFollower.ts
  - src/game/GameRuntime.ts
files-modified:
  - src/rendering/HazardOverlayRenderer.ts
  - src/rendering/HazardOverlayRenderer.test.ts
  - src/systems/PathFollower.ts
  - src/game/GameRuntime.ts
autonomous: true
requirements:
  - UI-03
  - OBST-04
must_haves:
  truths:
    - Hazard/urgency visuals remain legible at gameplay speed for collisions, distractions, and blocked states.
    - Reroutes issued during blocked states are preserved and applied seamlessly when artists resume movement.
  artifacts:
    - src/rendering/HazardOverlayRenderer.ts
    - src/rendering/HazardOverlayRenderer.test.ts
  key_links:
    - src/game/GameRuntime.ts wires collision/distraction status into hazard overlay rendering each frame.
    - src/systems/PathFollower.ts handles delayed reroute queue application after blocked-state release.
---

<objective>
Implement hazard readability overlays and blocked-state reroute handling.

Purpose: Keep collision/distraction urgency legible while ensuring player reroutes remain responsive even when artists are temporarily blocked.
Output: Hazard overlay renderer and runtime reroute queue application flow for blocked artists.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/game/GameRuntime.ts
@src/rendering/HudRenderer.ts
@src/rendering/DeliveryFeedbackRenderer.ts
@src/systems/CollisionSystem.ts
@src/systems/DistractionSystem.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement hazard overlay renderer for collision/distraction readability</name>
  <files>src/rendering/HazardOverlayRenderer.ts, src/rendering/HazardOverlayRenderer.test.ts</files>
  <action>Create overlays for chatting pairs, active distraction zones, and blocked-state urgency hints. Keep visuals lightweight and readable in portrait gameplay with deterministic renderer cleanup each frame.</action>
  <verify>npm test -- src/rendering/HazardOverlayRenderer.test.ts</verify>
  <done>Hazard states are visually legible without obstructing path routing interactions.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement blocked-state reroute queue application flow</name>
  <files>src/systems/PathFollower.ts, src/game/GameRuntime.ts</files>
  <action>Support path redraw requests while artists are blocked in chat/distraction by queueing pending path assignments and applying them immediately once blocked states clear. Preserve no-jump continuity from current artist position.</action>
  <verify>npm run build</verify>
  <done>Reroutes during blocked states are retained and applied deterministically on resume.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Integrate hazard overlays + reroute behavior end-to-end and run regression checks</name>
  <files>src/game/GameRuntime.ts</files>
  <action>Integrate hazard overlay rendering with collision/distraction runtime state and verify blocked-state reroute behavior coexists with stage occupancy and scoring loops.</action>
  <verify>npm run build && npm test</verify>
  <done>Phase 5 runtime presents readable hazards and stable reroute-on-resume behavior without regressions.</done>
</task>

</tasks>

<verification>
Build and full test suite pass with hazard readability and blocked-state reroute behavior validated.
</verification>

<success_criteria>
- [ ] Hazard states (chat/distraction/blocked urgency) are readable during active play.
- [ ] Reroutes during blocked states queue and apply after resume with no jumps.
- [ ] Collision/distraction integration remains stable alongside stage and scoring systems.
</success_criteria>

<output>
After completion, create `.planning/phases/05-collisions-and-distractions/05-03-SUMMARY.md`.
</output>

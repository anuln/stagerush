---
phase: 08-gov-ball-content-and-production-assets
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/assets/BundleManager.ts
  - src/assets/BundleManager.test.ts
  - src/assets/manifest.ts
  - src/main.ts
  - src/game/GameManager.ts
  - src/maps/MapLoader.ts
  - src/persistence/RunPersistence.ts
files-modified:
  - src/assets/BundleManager.ts
  - src/assets/BundleManager.test.ts
  - src/assets/manifest.ts
  - src/main.ts
  - src/game/GameManager.ts
  - src/maps/MapLoader.ts
  - src/persistence/RunPersistence.ts
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - Festival-specific assets are loaded and unloaded via explicit bundle lifecycle boundaries.
    - Bundle switching avoids stale texture/audio references and keeps boot bundle resident.
  artifacts:
    - src/assets/BundleManager.ts
    - src/assets/manifest.ts
    - src/assets/BundleManager.test.ts
  key_links:
    - src/main.ts initializes boot bundle first and mounts Gov Ball bundle only when entering gameplay.
    - src/game/GameManager.ts coordinates bundle lifecycle with screen-state transitions and retry/restart flows.
---

<objective>
Implement production asset bundle lifecycle (load/unload) for festival content.

Purpose: Ensure Gov Ball assets are managed as a distinct bundle with predictable memory behavior and clean transitions.
Output: Bundle manager, manifest wiring, and integration tests proving load/unload correctness.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-technical-spec.md
@src/main.ts
@src/maps/MapLoader.ts
@src/game/GameManager.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define festival asset manifest and bundle manager</name>
  <files>src/assets/manifest.ts, src/assets/BundleManager.ts</files>
  <action>Create explicit boot/festival bundle manifests and manager APIs for load, warm, unload, and status checks.</action>
  <verify>npm run build</verify>
  <done>Bundle manager can deterministically load/unload Gov Ball assets while retaining boot resources.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Integrate bundle lifecycle with app/game flow</name>
  <files>src/main.ts, src/game/GameManager.ts, src/maps/MapLoader.ts</files>
  <action>Wire bundle load/unload into boot and gameplay transitions (menu -> playing -> menu/restart) and ensure map/content loading uses bundle-aware paths.</action>
  <verify>npm run build</verify>
  <done>Asset lifecycle is synchronized with session state and avoids stale references after unload/reload.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add bundle lifecycle and persistence interplay tests</name>
  <files>src/assets/BundleManager.test.ts, src/persistence/RunPersistence.ts</files>
  <action>Add tests covering repeated load/unload cycles, idempotent lifecycle operations, and persistence safety when bundles are reloaded between sessions.</action>
  <verify>npm test -- src/assets/BundleManager.test.ts src/persistence/RunPersistence.test.ts</verify>
  <done>Bundle lifecycle behavior is regression-tested for stability and session continuity.</done>
</task>

</tasks>

<verification>
Build and bundle lifecycle tests pass with reliable boot/festival load-unload behavior and no stale-asset regressions.
</verification>

<success_criteria>
- [ ] Boot and Gov Ball bundles have explicit manifests and lifecycle control.
- [ ] Festival transitions correctly load/unload assets without stale references.
- [ ] Bundle lifecycle reliability is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-gov-ball-content-and-production-assets/08-03-SUMMARY.md`.
</output>

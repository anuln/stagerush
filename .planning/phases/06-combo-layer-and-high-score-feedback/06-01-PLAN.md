---
phase: 06-combo-layer-and-high-score-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/ScoreConfig.ts
  - src/game/ComboTracker.ts
  - src/game/ComboTracker.test.ts
  - src/game/ScoreManager.ts
  - src/game/ScoreManager.test.ts
  - src/game/GameRuntime.ts
files-modified:
  - src/config/ScoreConfig.ts
  - src/game/ComboTracker.ts
  - src/game/ComboTracker.test.ts
  - src/game/ScoreManager.ts
  - src/game/ScoreManager.test.ts
  - src/game/GameRuntime.ts
autonomous: true
requirements:
  - SCORE-03
  - SCORE-04
must_haves:
  truths:
    - Per-stage combo chains increment/reset/cap according to configured combo window and multiplier ladder.
    - Combo multiplier is applied on top of existing base tier-stage score for each completed delivery.
  artifacts:
    - src/game/ComboTracker.ts
    - src/game/ComboTracker.test.ts
    - src/game/ScoreManager.ts
  key_links:
    - src/game/GameRuntime.ts routes stage completion timestamps into ComboTracker before score registration.
    - src/game/ScoreManager.ts emits combo-enriched score events for UI feedback rendering.
---

<objective>
Implement combo chain state and integrate combo multipliers into scoring.

Purpose: Reward deliberate stage sequencing by increasing score output for consecutive deliveries to the same stage inside a fixed window.
Output: Combo tracker plus score-manager integration that applies multipliers deterministically and exposes combo metadata for UI.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-product-spec.md
@src/game/ScoreManager.ts
@src/game/GameRuntime.ts
@src/entities/StageState.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define combo config and tracker contracts</name>
  <files>src/config/ScoreConfig.ts, src/game/ComboTracker.ts</files>
  <action>Add combo tuning constants (window, multiplier ladder, cap behavior) and implement per-stage combo state tracker keyed by stage id with delivery-time based updates.</action>
  <verify>npm run build</verify>
  <done>Combo tracker can resolve chain length and multiplier for any delivery timestamp deterministically.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Integrate combo multiplier into score manager and runtime event flow</name>
  <files>src/game/ScoreManager.ts, src/game/GameRuntime.ts</files>
  <action>Update score pipeline so each delivery first resolves combo state, then multiplies base tier-stage score by combo multiplier. Extend score event payloads with combo metadata for renderer/HUD consumers.</action>
  <verify>npm run build</verify>
  <done>Runtime scoring now reflects base × combo with stable cumulative totals and combo context in events.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add combo and score integration tests</name>
  <files>src/game/ComboTracker.test.ts, src/game/ScoreManager.test.ts</files>
  <action>Write tests for chain increment/reset/cap behavior, window expiry handling, and score outputs proving combo multipliers stack on base matrix values exactly.</action>
  <verify>npm test -- src/game/ComboTracker.test.ts src/game/ScoreManager.test.ts</verify>
  <done>Combo and scoring rules are regression-tested with precise expected point outputs.</done>
</task>

</tasks>

<verification>
Build and combo/score tests pass with correct chain progression and multiplier-applied score totals.
</verification>

<success_criteria>
- [ ] Combo chain behavior increments/resets/caps exactly by rules.
- [ ] Combo multiplier is applied on top of base tier-stage score.
- [ ] Runtime score events include combo metadata for feedback systems.
</success_criteria>

<output>
After completion, create `.planning/phases/06-combo-layer-and-high-score-feedback/06-01-SUMMARY.md`.
</output>

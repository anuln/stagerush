---
phase: 09-performance-qa-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/GameConfig.ts
  - src/game/GameRuntime.ts
  - src/main.ts
  - src/debug/PerformanceOverlay.ts
  - scripts/profile-runtime.mjs
  - package.json
files-modified:
  - src/config/GameConfig.ts
  - src/game/GameRuntime.ts
  - src/main.ts
  - src/debug/PerformanceOverlay.ts
  - scripts/profile-runtime.mjs
  - package.json
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - Runtime performance metrics (frame time, update budget, load timing) are measurable through reproducible profiling commands.
    - Quality scaling can be toggled by measured thresholds without breaking gameplay readability.
  artifacts:
    - src/debug/PerformanceOverlay.ts
    - scripts/profile-runtime.mjs
  key_links:
    - src/main.ts initializes profiling hooks and quality scaling gates behind debug-safe flags.
    - src/game/GameRuntime.ts exposes lightweight per-frame metrics needed for tuning without introducing heavy overhead.
---

<objective>
Establish performance instrumentation and quality-scaler controls for mobile-target runtime behavior.

Purpose: Generate reliable evidence for frame stability, input responsiveness, load time, and payload budgets before release hardening.
Output: Profiling pipeline, runtime metrics surfacing, and measured quality controls integrated into gameplay.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/main.ts
@src/game/GameRuntime.ts
@src/config/GameConfig.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add runtime performance telemetry hooks</name>
  <files>src/game/GameRuntime.ts, src/debug/PerformanceOverlay.ts, src/main.ts</files>
  <action>Add low-overhead telemetry for frame/update timing, spawn/load pressure, and runtime-state counters with an opt-in on-screen/debug output.</action>
  <verify>npm run build</verify>
  <done>Performance telemetry is available for profiling sessions without changing default gameplay behavior.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement quality-scaler strategy for performance guardrails</name>
  <files>src/config/GameConfig.ts, src/main.ts, src/game/GameRuntime.ts</files>
  <action>Introduce measured quality presets (resolution/fx intensity limits) and apply threshold-based scaling for sustained low FPS conditions.</action>
  <verify>npm run build && npm test</verify>
  <done>Runtime can degrade and recover visual quality safely while preserving control responsiveness and gameplay legibility.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add reproducible profiling script and baseline capture</name>
  <files>scripts/profile-runtime.mjs, package.json</files>
  <action>Create a commandable profiling script that records frame-time and startup/load metrics to a report artifact for repeatable comparisons.</action>
  <verify>npm run perf:profile</verify>
  <done>Profiling can be run consistently in local/CI contexts and outputs a baseline report used by later release checks.</done>
</task>

</tasks>

<verification>
Profiling command produces baseline metrics; build and tests pass; quality scaling behavior is measurable and non-regressive.
</verification>

<success_criteria>
- [ ] Runtime metrics collection exists and is reproducible.
- [ ] Quality scaler can mitigate low-performance conditions without gameplay regressions.
- [ ] Baseline data exists for FPS, startup timing, and payload budget tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-qa-and-release-hardening/09-01-SUMMARY.md`.
</output>

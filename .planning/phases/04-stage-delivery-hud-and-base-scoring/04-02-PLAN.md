---
phase: 04-stage-delivery-hud-and-base-scoring
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/config/ScoreConfig.ts
  - src/game/ScoreManager.ts
  - src/game/ScoreManager.test.ts
  - src/game/GameRuntime.ts
files-modified:
  - src/config/ScoreConfig.ts
  - src/game/ScoreManager.ts
  - src/game/ScoreManager.test.ts
  - src/game/GameRuntime.ts
autonomous: true
requirements:
  - SCORE-01
must_haves:
  truths:
    - Score calculation applies tier-stage matrix values exactly as defined by the product spec baseline.
    - Runtime score total updates only on completed stage deliveries and remains deterministic.
  artifacts:
    - src/config/ScoreConfig.ts
    - src/game/ScoreManager.ts
    - src/game/ScoreManager.test.ts
  key_links:
    - src/game/ScoreManager.ts consumes delivery completion events from StageSystem.
    - src/game/GameRuntime.ts records score events for HUD feedback in 04-03.
---

<objective>
Implement matrix-based delivery scoring and total-score accumulation.

Purpose: Translate completed stage performances into predictable points that reward correct artist-tier to stage-size matching.
Output: Score manager with exact matrix mapping, running total, and score event emission for UI consumers.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-product-spec.md
@src/systems/StageSystem.ts
@src/game/GameRuntime.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define scoring matrix config and score event contracts</name>
  <files>src/config/ScoreConfig.ts, src/game/ScoreManager.ts</files>
  <action>Create explicit tier-stage matrix constants and score event payload contracts. Include baseline matrix values (headliner/midtier/newcomer vs large/medium/small) and pure helper logic for per-delivery score resolution.</action>
  <verify>npm run build</verify>
  <done>Matrix values are centralized and accessible through a deterministic scoring API.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement running total updates from delivery completion events</name>
  <files>src/game/ScoreManager.ts, src/game/GameRuntime.ts</files>
  <action>Consume stage delivery-complete events, apply matrix scoring per completed artist delivery, update running total, and store the latest score event for downstream HUD/feedback rendering.</action>
  <verify>npm run build</verify>
  <done>Runtime score total increments exactly when artists complete stage performances.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add score-manager tests for matrix correctness and totals</name>
  <files>src/game/ScoreManager.test.ts</files>
  <action>Add tests covering all tier-stage matrix combinations, cumulative total updates across multiple deliveries, and stable event payload shape for UI hooks.</action>
  <verify>npm test -- src/game/ScoreManager.test.ts</verify>
  <done>Scoring behavior is test-backed and exact against matrix rules.</done>
</task>

</tasks>

<verification>
Build and score-manager tests pass with matrix-accurate score resolution and deterministic running totals.
</verification>

<success_criteria>
- [ ] Tier-stage scoring matrix is implemented exactly and tested.
- [ ] Score total updates from completed stage deliveries only.
- [ ] Score events are available for HUD and delivery feedback hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stage-delivery-hud-and-base-scoring/04-02-SUMMARY.md`.
</output>

---
phase: 02-artist-lifecycle-and-spawn-pressure
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/rendering/ArtistRenderer.ts
  - src/rendering/TimerRingRenderer.ts
  - src/game/LivesState.ts
  - src/game/GameRuntime.ts
  - src/main.ts
  - src/game/LivesState.test.ts
files-modified:
  - src/rendering/ArtistRenderer.ts
  - src/rendering/TimerRingRenderer.ts
  - src/game/LivesState.ts
  - src/game/GameRuntime.ts
  - src/main.ts
  - src/game/LivesState.test.ts
autonomous: true
requirements:
  - CORE-05
  - CORE-06
must_haves:
  truths:
    - Active artists render as tier-based placeholder visuals with visible countdown urgency rings.
    - Miss events decrement lives and trigger level-fail signal at three misses.
  artifacts:
    - src/rendering/ArtistRenderer.ts
    - src/rendering/TimerRingRenderer.ts
    - src/game/LivesState.ts
    - src/game/GameRuntime.ts
  key_links:
    - src/game/GameRuntime.ts wires SpawnSystem and TimerSystem outputs into rendering + lives state.
    - src/game/LivesState.ts receives miss events from TimerSystem and exposes level-fail readiness at threshold.
---

<objective>
Integrate artist placeholders and miss/lives runtime plumbing.

Purpose: Make Phase 2 behavior visible and actionable by rendering active artists/timers and connecting timeout/bounds misses to a life counter that can end the level.
Output: Artist/timer placeholder rendering plus runtime game-state handling for misses and lives depletion.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/main.ts
@src/maps/layers.ts
@src/systems/SpawnSystem.ts
@src/systems/TimerSystem.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add artist and timer ring placeholder renderers</name>
  <files>src/rendering/ArtistRenderer.ts, src/rendering/TimerRingRenderer.ts, src/maps/layers.ts</files>
  <action>Create renderer utilities that draw tier-based placeholder artist markers and circular timer rings with green→yellow→red urgency transitions. Extend layer setup if needed so artist visuals render above paths/map but below HUD layers.</action>
  <verify>npm run build</verify>
  <done>Active artists and their timer rings can be drawn/updated each frame with clear urgency coloring.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement lives state and miss-threshold logic</name>
  <files>src/game/LivesState.ts, src/game/LivesState.test.ts</files>
  <action>Implement lives tracker starting at 3 misses per level, with decrement on miss events and a deterministic `isLevelFailed` state when count reaches zero. Add tests for decrement behavior, non-negative bounds, and fail threshold transitions.</action>
  <verify>npm test -- src/game/LivesState.test.ts</verify>
  <done>Lives state transitions are test-backed and provide clean signals for runtime flow control.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire phase runtime loop for spawn, update, render, and misses</name>
  <files>src/game/GameRuntime.ts, src/main.ts</files>
  <action>Introduce a temporary runtime orchestrator that updates SpawnSystem, Artist entities, TimerSystem, and renderers each ticker tick. Connect miss events to lives tracker and log/emit level-fail signal when lives are exhausted. Keep integration modular to ease future GameManager replacement.</action>
  <verify>npm run build && npm test</verify>
  <done>Phase 2 loop visibly spawns drifting artists, updates timer rings, and tracks misses into a 3-life failure condition.</done>
</task>

</tasks>

<verification>
Build and tests pass, and runtime smoke-check confirms artist/timer rendering plus life depletion behavior under timeout/miss conditions.
</verification>

<success_criteria>
- [ ] Tier placeholder artists and timer rings render and update each frame.
- [ ] Misses decrement lives and trigger fail signal at 3 misses.
- [ ] Phase 2 runtime integration is modular and ready for Phase 3 path-drawing attachment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artist-lifecycle-and-spawn-pressure/02-03-SUMMARY.md`.
</output>

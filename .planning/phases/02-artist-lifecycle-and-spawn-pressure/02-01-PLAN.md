---
phase: 02-artist-lifecycle-and-spawn-pressure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/Artist.ts
  - src/entities/Artist.test.ts
  - src/entities/ArtistState.ts
  - src/utils/MathUtils.ts
files-modified:
  - src/entities/Artist.ts
  - src/entities/Artist.test.ts
  - src/entities/ArtistState.ts
  - src/utils/MathUtils.ts
autonomous: true
requirements:
  - CORE-01
must_haves:
  truths:
    - Artist entities support deterministic drift movement using delta-time updates.
    - Artist state model is explicit and ready for future FOLLOWING/CHATTING/DISTRACTED transitions.
  artifacts:
    - src/entities/Artist.ts
    - src/entities/ArtistState.ts
    - src/entities/Artist.test.ts
  key_links:
    - src/entities/Artist.ts imports shared state/type contracts from src/entities/ArtistState.ts.
    - src/entities/Artist.test.ts validates delta-time drift behavior from src/entities/Artist.ts.
---

<objective>
Implement the core artist entity lifecycle baseline.

Purpose: Establish the canonical data model and update loop for all artist behavior so spawn, timer, pathing, and collision systems can build on one reliable entity contract.
Output: `Artist` entity class with state machine-ready transitions, drift updates, and deterministic movement tests.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/config/FestivalConfig.ts
@src/maps/MapLoader.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define artist state and domain contracts</name>
  <files>src/entities/ArtistState.ts</files>
  <action>Create strongly typed artist state enums/interfaces for lifecycle phases (SPAWNING, DRIFTING, FOLLOWING, CHATTING, DISTRACTED, ARRIVING, PERFORMING, COMPLETED, MISSED) plus baseline artist construction/update contracts used by systems.</action>
  <verify>npm run build</verify>
  <done>State and contract types compile and can be imported by runtime systems without type ambiguity.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement Artist entity with delta-time drift updates</name>
  <files>src/entities/Artist.ts, src/utils/MathUtils.ts</files>
  <action>Implement Artist class with id/tier/position/velocity/state/timer fields, delta-time movement update, bounds checks, and transition helpers for missed/completed state handling. Add minimal shared math helpers for vector/position operations if needed.</action>
  <verify>npm run build</verify>
  <done>Artist instances drift deterministically from initial velocity and expose transition-safe methods for later systems.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add unit tests for drift and state transitions</name>
  <files>src/entities/Artist.test.ts</files>
  <action>Write focused tests for delta-time movement, timer decrement behavior, and key state transitions (drifting to missed on timeout/bounds). Keep tests deterministic and independent from rendering layers.</action>
  <verify>npm test -- src/entities/Artist.test.ts</verify>
  <done>Tests fail before implementation and pass after, proving artist movement and state transition logic correctness.</done>
</task>

</tasks>

<verification>
Build and targeted entity tests pass, and artist contracts are ready for spawn/timer systems to consume.
</verification>

<success_criteria>
- [ ] Artist domain model and state contracts are implemented.
- [ ] Delta-time drift behavior is deterministic and test-backed.
- [ ] Entity API is stable for spawn/timer integration in next plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artist-lifecycle-and-spawn-pressure/02-01-SUMMARY.md`.
</output>

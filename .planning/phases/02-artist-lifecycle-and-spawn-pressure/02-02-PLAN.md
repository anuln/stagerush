---
phase: 02-artist-lifecycle-and-spawn-pressure
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/systems/SpawnSystem.ts
  - src/systems/TimerSystem.ts
  - src/config/LevelConfig.ts
  - src/systems/SpawnSystem.test.ts
  - src/systems/TimerSystem.test.ts
files-modified:
  - src/systems/SpawnSystem.ts
  - src/systems/TimerSystem.ts
  - src/config/LevelConfig.ts
  - src/systems/SpawnSystem.test.ts
  - src/systems/TimerSystem.test.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-06
must_haves:
  truths:
    - Spawn system enforces max simultaneous artist limits and level-driven spawn cadence.
    - Timer system marks artists as missed when timers expire and emits miss-ready signals.
  artifacts:
    - src/systems/SpawnSystem.ts
    - src/systems/TimerSystem.ts
    - src/config/LevelConfig.ts
  key_links:
    - src/systems/SpawnSystem.ts creates Artist instances using contracts from src/entities/Artist.ts.
    - src/systems/TimerSystem.ts transitions Artist entities to MISSED and exposes miss events for lives tracking.
---

<objective>
Add spawn orchestration and timer pressure systems.

Purpose: Convert static map/spawn data into active artist traffic while enforcing timeout-based miss conditions required for level pressure.
Output: Configurable SpawnSystem and TimerSystem with test coverage for simultaneous limits, spawn cadence, and miss transitions.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/entities/Artist.ts
@src/maps/MapLoader.ts
@public/assets/maps/govball/config.json
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define level runtime configuration contracts</name>
  <files>src/config/LevelConfig.ts</files>
  <action>Create strongly typed LevelConfig interfaces for total artists, maxSimultaneous, timer ranges, spawn intervals, and tier weights. Keep contract aligned with technical spec and compatible with future LevelManager integration.</action>
  <verify>npm run build</verify>
  <done>Runtime level configuration types compile and are reusable by spawn/timer systems.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement SpawnSystem with simultaneous-cap enforcement</name>
  <files>src/systems/SpawnSystem.ts, src/systems/SpawnSystem.test.ts</files>
  <action>Implement SpawnSystem that consumes level config + spawn points, tracks active artists, schedules spawns within interval ranges, and blocks spawns when maxSimultaneous is reached. Add tests for spawn quotas, cadence windows, and deterministic selection seams.</action>
  <verify>npm test -- src/systems/SpawnSystem.test.ts</verify>
  <done>SpawnSystem produces valid artists at expected cadence and never exceeds configured active caps.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Implement TimerSystem and miss transitions</name>
  <files>src/systems/TimerSystem.ts, src/systems/TimerSystem.test.ts</files>
  <action>Implement TimerSystem to decrement all active artist timers each update, mark timeouts as MISSED, and expose miss events/hooks for lives tracking. Add tests confirming expiry transitions and no false misses before timeout.</action>
  <verify>npm test -- src/systems/TimerSystem.test.ts</verify>
  <done>Timer expiry reliably causes missed-state transitions and emits miss events suitable for game-state accounting.</done>
</task>

</tasks>

<verification>
Targeted system tests pass and build remains clean with SpawnSystem + TimerSystem integrated.
</verification>

<success_criteria>
- [ ] SpawnSystem enforces maxSimultaneous and spawn cadence rules.
- [ ] TimerSystem transitions timeout cases to MISSED deterministically.
- [ ] Miss signaling path is available for lives UI/state in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-artist-lifecycle-and-spawn-pressure/02-02-SUMMARY.md`.
</output>

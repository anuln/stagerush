---
phase: 07-level-flow-and-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/config/LevelConfig.ts
  - src/game/LevelProgression.ts
  - src/game/LevelProgression.test.ts
  - src/game/LevelManager.ts
  - src/systems/SpawnSystem.ts
  - src/systems/SpawnSystem.test.ts
files-modified:
  - src/config/LevelConfig.ts
  - src/game/LevelProgression.ts
  - src/game/LevelProgression.test.ts
  - src/game/LevelManager.ts
  - src/systems/SpawnSystem.ts
  - src/systems/SpawnSystem.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
must_haves:
  truths:
    - Sequential level progression uses escalating parameters that can be resolved for each level number.
    - Attempt-level randomization varies spawn pressure and tier mix within configured bounds while remaining reproducible per seed.
  artifacts:
    - src/game/LevelProgression.ts
    - src/game/LevelProgression.test.ts
    - src/game/LevelManager.ts
  key_links:
    - src/game/LevelManager.ts consumes progression/randomization outputs to instantiate each level attempt.
    - src/systems/SpawnSystem.ts accepts randomized level parameters without regressing baseline spawn behavior.
---

<objective>
Implement level escalation and controlled randomization for replayable attempts.

Purpose: Deliver difficulty growth across a festival while keeping runs varied enough to avoid deterministic repetition.
Output: Progression + randomization modules wired into LevelManager and spawn/timer configuration.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-product-spec.md
@src/config/LevelConfig.ts
@src/game/LevelManager.ts
@src/systems/SpawnSystem.ts
@public/assets/maps/govball/config.json
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement progression and randomization primitives</name>
  <files>src/game/LevelProgression.ts, src/config/LevelConfig.ts</files>
  <action>Add deterministic helpers that resolve per-level target parameters and bounded randomization seeds (spawn interval, timer ranges, tier weights, distraction subsets).</action>
  <verify>npm run build</verify>
  <done>Progression primitives can compute level configs from map data and seed inputs predictably.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire progression outputs into LevelManager and spawn flow</name>
  <files>src/game/LevelManager.ts, src/systems/SpawnSystem.ts</files>
  <action>Update LevelManager setup so each attempt resolves randomized-but-bounded runtime level config and propagates it into spawn/timer behavior.</action>
  <verify>npm run build</verify>
  <done>Level attempts now use escalating and varied runtime parameters while preserving existing gameplay rules.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add progression/randomization regression tests</name>
  <files>src/game/LevelProgression.test.ts, src/systems/SpawnSystem.test.ts</files>
  <action>Test level escalation boundaries, seed reproducibility, and acceptable variance windows to ensure controlled randomization never violates design limits.</action>
  <verify>npm test -- src/game/LevelProgression.test.ts src/systems/SpawnSystem.test.ts</verify>
  <done>Progression and attempt randomization are deterministic where required and bounded by tests.</done>
</task>

</tasks>

<verification>
Build and targeted progression tests pass with reproducible seeded variation and level-by-level escalation outputs.
</verification>

<success_criteria>
- [ ] Level progression parameters escalate across festival levels.
- [ ] Attempt randomization is controlled, reproducible, and bounded.
- [ ] Spawn/timer/tier behavior reflects progression outputs without regression.
</success_criteria>

<output>
After completion, create `.planning/phases/07-level-flow-and-persistence/07-02-SUMMARY.md`.
</output>

---
phase: 07-level-flow-and-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/ui/ScreenState.ts
  - src/ui/ScreenViewModels.ts
  - src/ui/ScreenViewModels.test.ts
  - src/ui/ScreenOverlayController.ts
  - src/game/GameManager.ts
  - src/main.ts
files-modified:
  - src/ui/ScreenState.ts
  - src/ui/ScreenViewModels.ts
  - src/ui/ScreenViewModels.test.ts
  - src/ui/ScreenOverlayController.ts
  - src/game/GameManager.ts
  - src/main.ts
autonomous: true
requirements:
  - PROG-04
  - UI-04
must_haves:
  truths:
    - Non-gameplay screens exist for menu, level-failed, level-complete, and festival-complete states with explicit next actions.
    - Result summaries expose per-level and run-level breakdown data needed for player feedback and future leaderboard use.
  artifacts:
    - src/ui/ScreenOverlayController.ts
    - src/ui/ScreenViewModels.ts
    - src/ui/ScreenViewModels.test.ts
  key_links:
    - src/game/GameManager.ts is the single source of truth for screen-state transitions.
    - src/main.ts mounts and updates overlay screens alongside Pixi runtime updates.
---

<objective>
Build non-gameplay UX flow and result summaries for full session continuity.

Purpose: Complete the player-facing loop with clear screen transitions and actionable outcomes beyond active gameplay.
Output: Overlay screen system driven by GameManager state and summary view models.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-product-spec.md
@src/main.ts
@src/game/GameManager.ts
@src/game/ScoreManager.ts
@src/styles.css
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define screen state contracts and summary view models</name>
  <files>src/ui/ScreenState.ts, src/ui/ScreenViewModels.ts</files>
  <action>Create typed screen-state and summary payload contracts for menu, fail, level-complete, and festival-complete flows.</action>
  <verify>npm run build</verify>
  <done>View-model contracts can represent all required non-gameplay screens and actions.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement and wire screen overlay controller</name>
  <files>src/ui/ScreenOverlayController.ts, src/game/GameManager.ts, src/main.ts</files>
  <action>Implement a DOM/Pixi-safe overlay controller that renders actionable screens and bind it to GameManager transition events.</action>
  <verify>npm run build</verify>
  <done>Players can move through menu/play/fail/complete flows with clear next actions from overlays.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add summary and screen transition tests</name>
  <files>src/ui/ScreenViewModels.test.ts, src/game/GameManager.test.ts</files>
  <action>Test view-model formatting and state transitions for replay, next-level, and festival completion navigation paths.</action>
  <verify>npm test -- src/ui/ScreenViewModels.test.ts src/game/GameManager.test.ts</verify>
  <done>Screen flows and summary payloads are regression-tested for required paths.</done>
</task>

</tasks>

<verification>
Build and screen-focused tests pass with complete non-gameplay flow transitions and summary payload generation.
</verification>

<success_criteria>
- [ ] Menu/fail/complete screens are available and transition correctly.
- [ ] Result summaries provide clear, actionable run context.
- [ ] Overlay wiring coexists with gameplay runtime without input/render regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-level-flow-and-persistence/07-03-SUMMARY.md`.
</output>

---
phase: 07-level-flow-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/GameManager.ts
  - src/game/GameManager.test.ts
  - src/game/LevelManager.ts
  - src/game/LevelManager.test.ts
  - src/game/GameRuntime.ts
  - src/main.ts
files-modified:
  - src/game/GameManager.ts
  - src/game/GameManager.test.ts
  - src/game/LevelManager.ts
  - src/game/LevelManager.test.ts
  - src/game/GameRuntime.ts
  - src/main.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-04
  - CORE-07
must_haves:
  truths:
    - Session orchestration supports level start, active play, level fail, retry, and next-level transitions.
    - Level fail is driven by three misses and exposes replay-or-quit actions without resetting global app state incorrectly.
  artifacts:
    - src/game/GameManager.ts
    - src/game/LevelManager.ts
    - src/game/GameManager.test.ts
  key_links:
    - src/main.ts delegates lifecycle and input routing through GameManager instead of directly mutating runtime objects.
    - src/game/GameRuntime.ts exposes terminal level outcome signals consumed by LevelManager.
---

<objective>
Implement foundational session state management for menu/play/fail/complete loop control.

Purpose: Move from single-level runtime boot to a structured game flow that can advance and replay levels deterministically.
Output: GameManager and LevelManager orchestration with tested transition behavior wired through app bootstrap.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-product-spec.md
@src/main.ts
@src/game/GameRuntime.ts
@src/game/LivesState.ts
@src/config/LevelConfig.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add game and level orchestration contracts</name>
  <files>src/game/GameManager.ts, src/game/LevelManager.ts</files>
  <action>Create manager types/state machines for app flow and per-level attempt lifecycle, including replay and quit paths after failure.</action>
  <verify>npm run build</verify>
  <done>Managers expose deterministic transition APIs for boot, start level, fail, retry, and advance.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire managers into bootstrap and runtime lifecycle</name>
  <files>src/main.ts, src/game/GameRuntime.ts, src/game/LevelManager.ts</files>
  <action>Refactor bootstrap to construct and tick through GameManager, and expose minimal runtime outcome/status signals so managers can drive level completion/failure routing.</action>
  <verify>npm run build</verify>
  <done>App loop runs through manager-owned state transitions without breaking pointer input and rendering updates.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add transition and fail-loop tests</name>
  <files>src/game/GameManager.test.ts, src/game/LevelManager.test.ts</files>
  <action>Write tests for level start, 3-miss failure handling, replay reset behavior, and next-level transition boundaries.</action>
  <verify>npm test -- src/game/GameManager.test.ts src/game/LevelManager.test.ts</verify>
  <done>State transitions and fail/replay flow are covered with regression-safe unit tests.</done>
</task>

</tasks>

<verification>
Build succeeds and new manager tests prove deterministic level flow, including fail and retry paths.
</verification>

<success_criteria>
- [ ] GameManager and LevelManager own level lifecycle transitions.
- [ ] 3-miss condition routes into level-failed flow with replay/quit options.
- [ ] Existing runtime input/render loop remains functional under manager orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-level-flow-and-persistence/07-01-SUMMARY.md`.
</output>

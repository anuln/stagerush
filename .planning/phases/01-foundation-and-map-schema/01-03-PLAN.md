---
phase: 01-foundation-and-map-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/maps/MapRenderer.ts
  - src/maps/layers.ts
  - src/main.ts
  - src/debug/DebugToggles.ts
files-modified:
  - src/maps/MapRenderer.ts
  - src/maps/layers.ts
  - src/main.ts
  - src/debug/DebugToggles.ts
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - Map, stage markers, and spawn direction indicators render from config-driven data.
    - Resize events reproject normalized coordinates so layout remains consistent across viewport changes.
  artifacts:
    - src/maps/MapRenderer.ts
    - src/maps/layers.ts
    - src/debug/DebugToggles.ts
  key_links:
    - src/main.ts creates and updates renderer using MapLoader output.
    - src/maps/MapRenderer.ts consumes normalized-to-screen positions from src/maps/MapLoader.ts.
---

<objective>
Render the Phase 1 world view from configuration data.

Purpose: Produce the first visual map slice with stage and spawn debug elements so later entity/path systems can be layered onto a stable spatial foundation.
Output: Scene layers and map renderer integration that draw configurable stage blocks and spawn direction indicators with resize-safe reprojection.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@stage-call-product-spec.md
@stage-call-technical-spec.md
@stage-call-implementation-plan.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Build rendering layers and debug toggle controls</name>
  <files>src/maps/layers.ts, src/debug/DebugToggles.ts</files>
  <action>Define layer containers for map, stage, and debug visuals with deterministic draw order matching the technical spec. Add a simple toggle mechanism for spawn debug markers so debug rendering can be enabled/disabled without touching core rendering logic.</action>
  <verify>npm run build</verify>
  <done>Layer setup and debug toggles are reusable and imported by main runtime without circular dependencies.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement MapRenderer for stage and spawn visual primitives</name>
  <files>src/maps/MapRenderer.ts</files>
  <action>Render background placeholder, stage rectangles by size category, and spawn-point drift arrows based on loader output. Keep rendering config-driven and avoid hardcoded stage/spawn positions. Ensure markers remain visually clear for upcoming gameplay integration.</action>
  <verify>npm run build</verify>
  <done>Renderer produces all required visual primitives from config data and can be redrawn safely.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire renderer lifecycle and resize reprojection</name>
  <files>src/main.ts</files>
  <action>Integrate MapLoader + MapRenderer into the app bootstrap. On initial load and resize, recompute screen-space coordinates from normalized values and re-render scene layers. Keep this pathway deterministic and ready for additional entities in Phase 2.</action>
  <verify>npm run build</verify>
  <done>Resizing viewport preserves relative stage/spawn placement and no runtime exceptions occur during redraw.</done>
</task>

</tasks>

<verification>
Run build and manually confirm rendered map primitives remain correctly positioned after multiple viewport size changes.
</verification>

<success_criteria>
- [ ] Stage and spawn debug visuals are rendered from map configuration.
- [ ] Layering model is in place for future gameplay systems.
- [ ] Resize handling preserves normalized coordinate intent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-map-schema/01-03-SUMMARY.md`.
</output>

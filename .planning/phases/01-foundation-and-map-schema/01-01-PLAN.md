---
phase: 01-foundation-and-map-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.ts
  - src/styles.css
files-modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.ts
  - src/styles.css
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - App boots into a full-screen portrait PixiJS canvas without browser scroll/zoom interference.
    - Renderer initialization uses mobile-safe defaults from the technical spec.
  artifacts:
    - package.json
    - index.html
    - src/main.ts
    - src/styles.css
  key_links:
    - src/main.ts imports src/styles.css before app bootstrapping.
    - index.html loads the Vite entrypoint used by src/main.ts.
---

<objective>
Establish the runnable project shell for Stage Call.

Purpose: Create a stable PixiJS + TypeScript + Vite foundation so subsequent plans can focus on gameplay systems instead of tooling setup.
Output: A booting mobile-first Pixi app that renders a placeholder shape and compiles cleanly.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@stage-call-technical-spec.md
@stage-call-implementation-plan.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Scaffold Vite + TypeScript + PixiJS runtime</name>
  <files>package.json, tsconfig.json, vite.config.ts</files>
  <action>Initialize a Vite TypeScript project for browser runtime, add PixiJS v8 dependency, and ensure npm scripts include dev/build/preview flow. Keep project layout minimal and aligned with the technical spec directory strategy.</action>
  <verify>npm run build</verify>
  <done>Project compiles without errors and runtime dependencies are installed through package manifests.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement mobile-first app bootstrap</name>
  <files>index.html, src/main.ts, src/styles.css</files>
  <action>Create viewport/meta and CSS constraints for portrait mobile play (no margin, no scroll, touch-action none, user-select none). In main.ts, initialize Pixi Application with async init and mobile-oriented resolution/power preferences from technical spec, then render a visible placeholder rectangle for smoke validation.</action>
  <verify>npm run build</verify>
  <done>Running dev server shows full-screen canvas and placeholder render in portrait orientation with no console errors.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add resize-safe runtime wiring</name>
  <files>src/main.ts</files>
  <action>Ensure the app reacts to viewport resize using Pixi's resize strategy and keeps placeholder content visible. Preserve delta-time ticker setup needed by later gameplay systems.</action>
  <verify>npm run build</verify>
  <done>Canvas remains correctly sized after viewport changes and runtime loop is ready for system layering.</done>
</task>

</tasks>

<verification>
Confirm build succeeds and manual runtime smoke-check demonstrates full-screen canvas rendering with mobile-safe behavior.
</verification>

<success_criteria>
- [ ] Vite + TypeScript + PixiJS app shell exists and compiles.
- [ ] Portrait mobile viewport and touch-safe CSS constraints are active.
- [ ] Pixi renderer bootstraps with settings aligned to technical spec.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-map-schema/01-01-SUMMARY.md`.
</output>

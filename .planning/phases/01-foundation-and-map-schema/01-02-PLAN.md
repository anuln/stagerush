---
phase: 01-foundation-and-map-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/config/FestivalConfig.ts
  - src/config/GameConfig.ts
  - src/maps/MapLoader.ts
  - public/assets/maps/govball/config.json
  - src/maps/MapLoader.test.ts
  - package.json
files-modified:
  - src/config/FestivalConfig.ts
  - src/config/GameConfig.ts
  - src/maps/MapLoader.ts
  - public/assets/maps/govball/config.json
  - src/maps/MapLoader.test.ts
  - package.json
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - Festival map schema supports normalized positions for stages and spawn points.
    - Loader converts normalized coordinates into viewport-space positions deterministically.
  artifacts:
    - src/config/FestivalConfig.ts
    - src/maps/MapLoader.ts
    - public/assets/maps/govball/config.json
  key_links:
    - public/assets/maps/govball/config.json validates against src/config/FestivalConfig.ts types.
    - src/maps/MapLoader.ts applies normalization math using viewport dimensions from runtime.
---

<objective>
Define Phase 1 data contracts and loading behavior.

Purpose: Lock down festival schema and coordinate normalization early so rendering and gameplay systems share one source of truth.
Output: Typed FestivalMap contracts, an initial Gov Ball config file, and a loader that resolves normalized positions for rendering.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@stage-call-technical-spec.md
@stage-call-govball-assets.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Author FestivalMap and shared tuning contracts</name>
  <files>src/config/FestivalConfig.ts, src/config/GameConfig.ts</files>
  <action>Implement TypeScript interfaces for FestivalMap, StageConfig, SpawnPointConfig, DistractionConfig, and LevelConfig aligned with technical spec. Add centralized config constants for normalization helpers and default debug toggles needed in this phase.</action>
  <verify>npm run build</verify>
  <done>Schema and config types compile and are importable by map systems without type gaps.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Create initial Gov Ball map configuration payload</name>
  <files>public/assets/maps/govball/config.json</files>
  <action>Create a minimal but valid festival config for Phase 1 containing 3 stages and 4 spawn points with normalized coordinates and drift angles from the spec/asset sheet. Keep structure extensible for later level and distraction data.</action>
  <verify>npm run build</verify>
  <done>Gov Ball config exists at expected path and satisfies FestivalMap shape required for loader usage.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Implement MapLoader with normalization and basic tests</name>
  <files>src/maps/MapLoader.ts, src/maps/MapLoader.test.ts, package.json</files>
  <action>Implement loader utilities to fetch config, validate required fields, and transform normalized 0..1 positions into pixel coordinates for a supplied viewport. Add lightweight automated test coverage for normalization behavior and edge boundaries; wire test script if missing.</action>
  <verify>npm run build && npm test -- --runInBand</verify>
  <done>MapLoader returns stable screen-space outputs for known normalized inputs and tests pass locally.</done>
</task>

</tasks>

<verification>
Confirm schema/loader compile cleanly, test suite verifies normalization math, and Gov Ball config can be consumed without runtime parsing errors.
</verification>

<success_criteria>
- [ ] Festival data contracts are explicit and reusable.
- [ ] Gov Ball config is present and structurally valid.
- [ ] Loader converts normalized map coordinates to viewport coordinates predictably.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-map-schema/01-02-SUMMARY.md`.
</output>

---
phase: 03-touch-path-drawing-and-following
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/input/PathDrawingInput.ts
  - src/input/PathDrawingInput.test.ts
  - src/game/GameRuntime.ts
files-modified:
  - src/input/PathDrawingInput.ts
  - src/input/PathDrawingInput.test.ts
  - src/game/GameRuntime.ts
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - Player can begin path drawing by touching near an active artist.
    - In-progress path points stream in real time while pointer/touch moves.
  artifacts:
    - src/input/PathDrawingInput.ts
    - src/input/PathDrawingInput.test.ts
  key_links:
    - src/game/GameRuntime.ts feeds current active artists into PathDrawingInput hit detection.
    - src/input/PathDrawingInput.ts exposes session lifecycle events for downstream smoothing/render systems.
---

<objective>
Implement touch gesture capture and artist grab targeting for path drawing.

Purpose: Establish responsive single-touch interaction entrypoint so the player can reliably select moving artists and begin route creation.
Output: Input module that manages drawing sessions (`start`, `move`, `end`) and artist grab validation radius logic.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/game/GameRuntime.ts
@src/entities/Artist.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define path drawing session contract and state machine</name>
  <files>src/input/PathDrawingInput.ts</files>
  <action>Create typed structures for drawing sessions (artist id, raw points, pointer state, timestamps) and lifecycle transitions for pointer down/move/up. Limit to single active drawing session.</action>
  <verify>npm run build</verify>
  <done>Input module exposes deterministic session lifecycle APIs and compile-time typed session state.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement artist grab radius targeting and pointer capture</name>
  <files>src/input/PathDrawingInput.ts, src/game/GameRuntime.ts</files>
  <action>Implement nearest-artist selection within configured grab radius (40px baseline), start drawing from artist center, and append move points while drawing. Integrate runtime pointer events to feed active artists into the input module each frame.</action>
  <verify>npm run build</verify>
  <done>Touch/pointer interaction starts only when an eligible artist is within range and emits continuous raw path points.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add unit tests for session transitions and grab behavior</name>
  <files>src/input/PathDrawingInput.test.ts</files>
  <action>Write tests validating successful grab, failed grab outside radius, session move accumulation, and clean session close on pointer up/cancel.</action>
  <verify>npm test -- src/input/PathDrawingInput.test.ts</verify>
  <done>Session and grab tests pass and prove reliable interaction gate behavior.</done>
</task>

</tasks>

<verification>
Build passes and input tests confirm artist-targeted drawing sessions are deterministic and single-touch constrained.
</verification>

<success_criteria>
- [ ] Player can reliably start a drawing session near active artists.
- [ ] Raw pointer points stream while drawing is active.
- [ ] Input session APIs are test-backed and ready for smoothing/snap processing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-touch-path-drawing-and-following/03-01-SUMMARY.md`.
</output>

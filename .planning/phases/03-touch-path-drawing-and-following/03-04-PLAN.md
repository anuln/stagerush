---
phase: 03-touch-path-drawing-and-following
plan: 04
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/systems/PathFollower.ts
  - src/systems/PathFollower.test.ts
  - src/rendering/EtaRenderer.ts
  - src/game/GameRuntime.ts
  - src/main.ts
files-modified:
  - src/systems/PathFollower.ts
  - src/systems/PathFollower.test.ts
  - src/rendering/EtaRenderer.ts
  - src/game/GameRuntime.ts
  - src/main.ts
autonomous: true
requirements:
  - CORE-04
  - UI-02
must_haves:
  truths:
    - Artists follow finalized paths at consistent speed and support path replacement from current position.
    - ETA indicator appears while drawing and warns when ETA exceeds remaining timer.
  artifacts:
    - src/systems/PathFollower.ts
    - src/systems/PathFollower.test.ts
    - src/rendering/EtaRenderer.ts
  key_links:
    - src/game/GameRuntime.ts wires path finalization to PathFollower assignment and updates ETA overlays during draw.
    - src/systems/PathFollower.ts consumes path points from PathPlanner output and updates artist position deterministically.
---

<objective>
Integrate path following and ETA warning feedback into the runtime loop.

Purpose: Complete the Phase 3 gameplay loop by turning finalized routes into actual artist movement while giving players immediate timing feedback during path drawing.
Output: PathFollower system, path replacement handling, and ETA rendering with red warning state when route time exceeds remaining timer.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/entities/Artist.ts
@src/game/PathPlanner.ts
@src/game/GameRuntime.ts
@src/rendering/PathRenderer.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement path follower movement and reassignment</name>
  <files>src/systems/PathFollower.ts, src/systems/PathFollower.test.ts</files>
  <action>Implement system that moves artists along smoothed path points at constant speed, tracks path progress, and supports replacing an active path from the artist's current position. Add tests for completion, reassignment, and no-jump continuity.</action>
  <verify>npm test -- src/systems/PathFollower.test.ts</verify>
  <done>Artists can follow and replace paths seamlessly with deterministic movement progression.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement ETA overlay renderer with warning state</name>
  <files>src/rendering/EtaRenderer.ts, src/game/GameRuntime.ts</files>
  <action>Add ETA calculations based on path length and artist speed during active drawing sessions. Render ETA label near selected artist and switch to warning style when ETA exceeds timer remaining seconds.</action>
  <verify>npm run build</verify>
  <done>ETA overlay appears while drawing and visually warns on likely-late routes.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Wire end-to-end phase loop and runtime smoke checks</name>
  <files>src/game/GameRuntime.ts, src/main.ts</files>
  <action>Connect input sessions, path planning, path rendering lifecycle, path follower updates, and ETA rendering in the main game loop. Ensure invalid path finalization leaves prior movement intact and valid path replacements take effect immediately.</action>
  <verify>npm run build && npm test</verify>
  <done>Phase 3 loop supports draw→smooth/snap→follow with visible ETA warning and seamless path replacement.</done>
</task>

</tasks>

<verification>
Build and full test suite pass; runtime smoke check confirms the complete draw/follow/ETA interaction loop works as designed.
</verification>

<success_criteria>
- [ ] Artists follow finalized paths and support dynamic path replacement.
- [ ] ETA warning feedback is visible and accurate during drawing.
- [ ] Full Phase 3 interaction loop is integrated and stable for Phase 4 scoring work.
</success_criteria>

<output>
After completion, create `.planning/phases/03-touch-path-drawing-and-following/03-04-SUMMARY.md`.
</output>

---
phase: 03-touch-path-drawing-and-following
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/utils/Spline.ts
  - src/utils/Spline.test.ts
  - src/game/PathPlanner.ts
  - src/game/PathPlanner.test.ts
  - src/config/GameConfig.ts
files-modified:
  - src/utils/Spline.ts
  - src/utils/Spline.test.ts
  - src/game/PathPlanner.ts
  - src/game/PathPlanner.test.ts
  - src/config/GameConfig.ts
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - Finalized raw input points are smoothed/resampled into usable travel paths.
    - Path endpoint snaps to nearby stage entrance when within configurable snap radius.
  artifacts:
    - src/utils/Spline.ts
    - src/game/PathPlanner.ts
    - src/game/PathPlanner.test.ts
  key_links:
    - src/game/PathPlanner.ts consumes sessions from PathDrawingInput and stage data from resolved map layout.
    - src/utils/Spline.ts is the single smoothing implementation used by path planning.
---

<objective>
Implement spline smoothing, resampling, and stage snapping for finalized paths.

Purpose: Convert noisy touch trails into consistent movement-ready routes with stage-target inference that preserves player intent.
Output: Path planning pipeline that takes raw points and returns smoothed path data plus optional target stage id.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/maps/MapLoader.ts
@src/input/PathDrawingInput.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Implement Catmull-Rom spline and uniform resampling utilities</name>
  <files>src/utils/Spline.ts, src/utils/Spline.test.ts</files>
  <action>Build smoothing utilities for raw touch point arrays using Catmull-Rom interpolation and add uniform distance resampling helpers. Include tests for minimal point sets, monotonic stability, and predictable resample spacing.</action>
  <verify>npm test -- src/utils/Spline.test.ts</verify>
  <done>Spline utility returns stable smoothed/resampled points across representative path shapes.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement path finalization and stage snap logic</name>
  <files>src/game/PathPlanner.ts, src/config/GameConfig.ts</files>
  <action>Create planner that finalizes drawing sessions into path data (`rawPoints`, `smoothedPoints`, `length`, `targetStageId`). Apply configurable snap radius to stage entrances and extend endpoint to snapped stage when eligible.</action>
  <verify>npm run build</verify>
  <done>Planner output includes deterministic snap decision and movement-ready point list for follower system.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add integration tests for snap/no-snap scenarios</name>
  <files>src/game/PathPlanner.test.ts</files>
  <action>Test valid snap when final point is near stage, no-snap for distant endpoints, and output consistency for identical inputs. Verify planner emits the intended `targetStageId` contract.</action>
  <verify>npm test -- src/game/PathPlanner.test.ts</verify>
  <done>Path planner tests pass and cover critical snapping behavior expected by gameplay loop.</done>
</task>

</tasks>

<verification>
Build and planner/spline tests pass with deterministic smoothing and snap behavior.
</verification>

<success_criteria>
- [ ] Raw paths are smoothed and uniformly sampled for movement.
- [ ] Stage snapping works via configurable radius and target stage output.
- [ ] Finalization pipeline is test-backed and consumable by renderer/follower systems.
</success_criteria>

<output>
After completion, create `.planning/phases/03-touch-path-drawing-and-following/03-02-SUMMARY.md`.
</output>

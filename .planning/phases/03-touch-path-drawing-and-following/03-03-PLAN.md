---
phase: 03-touch-path-drawing-and-following
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/entities/PathState.ts
  - src/rendering/PathRenderer.ts
  - src/rendering/PathRenderer.test.ts
  - src/game/GameRuntime.ts
files-modified:
  - src/entities/PathState.ts
  - src/rendering/PathRenderer.ts
  - src/rendering/PathRenderer.test.ts
  - src/game/GameRuntime.ts
autonomous: true
requirements:
  - CORE-04
must_haves:
  truths:
    - Active paths render visibly and update lifecycle state as artists consume them.
    - Invalid unsnapped paths fade out without altering artist trajectory.
  artifacts:
    - src/entities/PathState.ts
    - src/rendering/PathRenderer.ts
    - src/rendering/PathRenderer.test.ts
  key_links:
    - src/game/GameRuntime.ts stores active path states and delegates visual updates to PathRenderer.
    - src/entities/PathState.ts tracks validity and fade lifecycle used by PathRenderer.
---

<objective>
Implement path rendering lifecycle including invalid-path fade behavior.

Purpose: Give players clear visual feedback on active routes while safely handling invalid drawings that should not disrupt movement behavior.
Output: Path state model plus renderer for in-progress/active/fading paths with invalid-path fade-out pipeline.
</objective>

<execution_context>
@/Users/anubhav.mehrotra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/anubhav.mehrotra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/game/PathPlanner.ts
@src/game/GameRuntime.ts
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Define path entity lifecycle and validity model</name>
  <files>src/entities/PathState.ts</files>
  <action>Create types/interfaces for path ownership, point arrays, stage targeting, consumed-progress metadata, and fade/expiry states. Include flags that distinguish valid snapped paths from invalid transient ones.</action>
  <verify>npm run build</verify>
  <done>Path lifecycle contracts are explicit and ready for rendering/follower integration.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Implement path renderer with active and fading visuals</name>
  <files>src/rendering/PathRenderer.ts, src/game/GameRuntime.ts</files>
  <action>Create renderer for in-progress and active path lines with stage-colored styling when snapped. Implement invalid-path fade timeline (0.5s baseline) that removes path visuals while preserving prior artist movement state when unsnapped.</action>
  <verify>npm run build</verify>
  <done>Runtime renders active paths and automatically fades/removes invalid paths without touching artist velocity/path assignment.</done>
</task>

## Task 3
<task type="auto">
  <name>Task 3: Add renderer tests for lifecycle transitions</name>
  <files>src/rendering/PathRenderer.test.ts</files>
  <action>Add tests verifying invalid path fade progression, active path persistence, and removal criteria after fade completion. Keep tests focused on lifecycle state transitions and renderer bookkeeping.</action>
  <verify>npm test -- src/rendering/PathRenderer.test.ts</verify>
  <done>Path renderer lifecycle behavior is test-backed for valid and invalid path states.</done>
</task>

</tasks>

<verification>
Build and path-renderer tests pass with validated invalid-path fade and active path persistence behavior.
</verification>

<success_criteria>
- [ ] Path lifecycle contracts model valid/invalid/fading states.
- [ ] Active paths are rendered consistently and invalid paths fade out safely.
- [ ] Invalid path handling does not alter existing artist trajectory state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-touch-path-drawing-and-following/03-03-SUMMARY.md`.
</output>
